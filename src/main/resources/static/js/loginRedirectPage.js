$(document).ready(() => {
    const $loginButton = $("#loginButton");
    $loginButton.click(() => {
        // console.log($("groupname").val());
        window.location.href = '/commonground';
    });
});
